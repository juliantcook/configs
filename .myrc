# my run commands

alias g='git'
alias ll='ls -laG'
alias d='docker'

if [ -n "${ZSH_VERSION:-}" ]; then
  PS1='%B%F{yellow}%2~ > %f%b'
elif [ -n "${BASH_VERSION:-}" ]; then
  PROMPT_DIRTRIM=2
  PS1='\[\e[1;33m\]\w > \[\e[0m\]'
fi

[ -f ~/.fzf ] && source ~/.fzf
